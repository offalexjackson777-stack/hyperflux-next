// Generated by ./hfx generate. Do not edit manually.
// SPDX-License-Identifier: GPL-2.0-only

#pragma once

#include <cstddef>
#include <cstdint>
#include <optional>
#include <string>
#include <string_view>
#include <utility>

namespace hyperflux
{

class GenerationId
{
public:
    using value_type = std::uint64_t;
    static constexpr value_type minimum = 1;
    static constexpr value_type maximum = 18446744073709551615ULL;

    [[nodiscard]] static constexpr std::optional<GenerationId> from(value_type value)
    {
        if(value < minimum)
        {
            return std::nullopt;
        }
        return GenerationId(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const GenerationId&, const GenerationId&) = default;

private:
    explicit constexpr GenerationId(value_type value) : value_(value) {}
    value_type value_;
};

class VendorId
{
public:
    using value_type = std::uint16_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 65535;

    [[nodiscard]] static constexpr std::optional<VendorId> from(value_type value)
    {
        return VendorId(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const VendorId&, const VendorId&) = default;

private:
    explicit constexpr VendorId(value_type value) : value_(value) {}
    value_type value_;
};

class ProductId
{
public:
    using value_type = std::uint16_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 65535;

    [[nodiscard]] static constexpr std::optional<ProductId> from(value_type value)
    {
        return ProductId(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const ProductId&, const ProductId&) = default;

private:
    explicit constexpr ProductId(value_type value) : value_(value) {}
    value_type value_;
};

class ProfileRevision
{
public:
    using value_type = std::uint32_t;
    static constexpr value_type minimum = 1;
    static constexpr value_type maximum = 4294967295;

    [[nodiscard]] static constexpr std::optional<ProfileRevision> from(value_type value)
    {
        if(value < minimum)
        {
            return std::nullopt;
        }
        return ProfileRevision(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const ProfileRevision&, const ProfileRevision&) = default;

private:
    explicit constexpr ProfileRevision(value_type value) : value_(value) {}
    value_type value_;
};

class BatteryPercent
{
public:
    using value_type = std::uint8_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 100;

    [[nodiscard]] static constexpr std::optional<BatteryPercent> from(value_type value)
    {
        if(value > maximum)
        {
            return std::nullopt;
        }
        return BatteryPercent(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const BatteryPercent&, const BatteryPercent&) = default;

private:
    explicit constexpr BatteryPercent(value_type value) : value_(value) {}
    value_type value_;
};

class Brightness
{
public:
    using value_type = std::uint8_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 255;

    [[nodiscard]] static constexpr std::optional<Brightness> from(value_type value)
    {
        return Brightness(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const Brightness&, const Brightness&) = default;

private:
    explicit constexpr Brightness(value_type value) : value_(value) {}
    value_type value_;
};

class DurationMs
{
public:
    using value_type = std::uint64_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 86400000ULL;

    [[nodiscard]] static constexpr std::optional<DurationMs> from(value_type value)
    {
        if(value > maximum)
        {
            return std::nullopt;
        }
        return DurationMs(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const DurationMs&, const DurationMs&) = default;

private:
    explicit constexpr DurationMs(value_type value) : value_(value) {}
    value_type value_;
};

class SequenceNumber
{
public:
    using value_type = std::uint64_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 18446744073709551615ULL;

    [[nodiscard]] static constexpr std::optional<SequenceNumber> from(value_type value)
    {
        return SequenceNumber(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const SequenceNumber&, const SequenceNumber&) = default;

private:
    explicit constexpr SequenceNumber(value_type value) : value_(value) {}
    value_type value_;
};

class LedIndex
{
public:
    using value_type = std::uint16_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 4095;

    [[nodiscard]] static constexpr std::optional<LedIndex> from(value_type value)
    {
        if(value > maximum)
        {
            return std::nullopt;
        }
        return LedIndex(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const LedIndex&, const LedIndex&) = default;

private:
    explicit constexpr LedIndex(value_type value) : value_(value) {}
    value_type value_;
};

class LedCount
{
public:
    using value_type = std::uint16_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 4096;

    [[nodiscard]] static constexpr std::optional<LedCount> from(value_type value)
    {
        if(value > maximum)
        {
            return std::nullopt;
        }
        return LedCount(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const LedCount&, const LedCount&) = default;

private:
    explicit constexpr LedCount(value_type value) : value_(value) {}
    value_type value_;
};

class CarrierIndex
{
public:
    using value_type = std::uint16_t;
    static constexpr value_type minimum = 0;
    static constexpr value_type maximum = 4095;

    [[nodiscard]] static constexpr std::optional<CarrierIndex> from(value_type value)
    {
        if(value > maximum)
        {
            return std::nullopt;
        }
        return CarrierIndex(value);
    }

    [[nodiscard]] constexpr value_type value() const { return value_; }
    friend constexpr bool operator==(const CarrierIndex&, const CarrierIndex&) = default;

private:
    explicit constexpr CarrierIndex(value_type value) : value_(value) {}
    value_type value_;
};

class ReceiverId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<ReceiverId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return ReceiverId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const ReceiverId&, const ReceiverId&) = default;

private:
    explicit ReceiverId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class LogicalDeviceId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<LogicalDeviceId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return LogicalDeviceId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const LogicalDeviceId&, const LogicalDeviceId&) = default;

private:
    explicit LogicalDeviceId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class EndpointId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<EndpointId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return EndpointId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const EndpointId&, const EndpointId&) = default;

private:
    explicit EndpointId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class LeaseId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<LeaseId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return LeaseId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const LeaseId&, const LeaseId&) = default;

private:
    explicit LeaseId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class TransactionId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<TransactionId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return TransactionId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const TransactionId&, const TransactionId&) = default;

private:
    explicit TransactionId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class ProfileId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<ProfileId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return ProfileId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const ProfileId&, const ProfileId&) = default;

private:
    explicit ProfileId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class CapabilityId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 128;

    [[nodiscard]] static std::optional<CapabilityId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return CapabilityId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const CapabilityId&, const CapabilityId&) = default;

private:
    explicit CapabilityId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

class EvidenceClaimId
{
public:
    static constexpr std::size_t minimum_length = 1;
    static constexpr std::size_t maximum_length = 160;

    [[nodiscard]] static std::optional<EvidenceClaimId> from(std::string_view value)
    {
        if(value.size() < minimum_length || value.size() > maximum_length)
        {
            return std::nullopt;
        }
        return EvidenceClaimId(std::string(value));
    }

    [[nodiscard]] std::string_view value() const { return value_; }
    friend bool operator==(const EvidenceClaimId&, const EvidenceClaimId&) = default;

private:
    explicit EvidenceClaimId(std::string value) : value_(std::move(value)) {}
    std::string value_;
};

enum class DeviceKind
{
    Receiver,
    Mat,
    Mouse,
    Keyboard,
    Unknown,
};

[[nodiscard]] constexpr std::string_view to_string(DeviceKind value)
{
    switch(value)
    {
        case DeviceKind::Receiver: return "receiver";
        case DeviceKind::Mat: return "mat";
        case DeviceKind::Mouse: return "mouse";
        case DeviceKind::Keyboard: return "keyboard";
        case DeviceKind::Unknown: return "unknown";
    }
    return "unknown";
}

enum class ProfileKind
{
    Receiver,
    Child,
    Surface,
};

[[nodiscard]] constexpr std::string_view to_string(ProfileKind value)
{
    switch(value)
    {
        case ProfileKind::Receiver: return "receiver";
        case ProfileKind::Child: return "child";
        case ProfileKind::Surface: return "surface";
    }
    return "unknown";
}

enum class RouteKind
{
    HyperfluxWireless,
    DirectUsb,
    Bluetooth,
};

[[nodiscard]] constexpr std::string_view to_string(RouteKind value)
{
    switch(value)
    {
        case RouteKind::HyperfluxWireless: return "hyperflux-wireless";
        case RouteKind::DirectUsb: return "direct-usb";
        case RouteKind::Bluetooth: return "bluetooth";
    }
    return "unknown";
}

enum class PairingState
{
    Paired,
    Unpaired,
    Unknown,
};

[[nodiscard]] constexpr std::string_view to_string(PairingState value)
{
    switch(value)
    {
        case PairingState::Paired: return "paired";
        case PairingState::Unpaired: return "unpaired";
        case PairingState::Unknown: return "unknown";
    }
    return "unknown";
}

enum class PresenceState
{
    Available,
    Sleeping,
    Unavailable,
    Unknown,
};

[[nodiscard]] constexpr std::string_view to_string(PresenceState value)
{
    switch(value)
    {
        case PresenceState::Available: return "available";
        case PresenceState::Sleeping: return "sleeping";
        case PresenceState::Unavailable: return "unavailable";
        case PresenceState::Unknown: return "unknown";
    }
    return "unknown";
}

enum class ConnectionMode
{
    Hyperflux24ghz,
    DirectUsb,
    Bluetooth,
    Unknown,
};

[[nodiscard]] constexpr std::string_view to_string(ConnectionMode value)
{
    switch(value)
    {
        case ConnectionMode::Hyperflux24ghz: return "hyperflux-2.4ghz";
        case ConnectionMode::DirectUsb: return "direct-usb";
        case ConnectionMode::Bluetooth: return "bluetooth";
        case ConnectionMode::Unknown: return "unknown";
    }
    return "unknown";
}

enum class EvidenceConfidence
{
    Observed,
    Derived,
    Inferred,
    Unknown,
};

[[nodiscard]] constexpr std::string_view to_string(EvidenceConfidence value)
{
    switch(value)
    {
        case EvidenceConfidence::Observed: return "observed";
        case EvidenceConfidence::Derived: return "derived";
        case EvidenceConfidence::Inferred: return "inferred";
        case EvidenceConfidence::Unknown: return "unknown";
    }
    return "unknown";
}

enum class EvidenceLevel
{
    Requirement,
    SourceReviewed,
    SimulationProven,
    HardwareObserved,
    HardwareQualified,
    ProductionQualified,
};

[[nodiscard]] constexpr std::string_view to_string(EvidenceLevel value)
{
    switch(value)
    {
        case EvidenceLevel::Requirement: return "requirement";
        case EvidenceLevel::SourceReviewed: return "source-reviewed";
        case EvidenceLevel::SimulationProven: return "simulation-proven";
        case EvidenceLevel::HardwareObserved: return "hardware-observed";
        case EvidenceLevel::HardwareQualified: return "hardware-qualified";
        case EvidenceLevel::ProductionQualified: return "production-qualified";
    }
    return "unknown";
}

enum class PrivacyClass
{
    Public,
    PublicSummary,
    Sensitive,
    Private,
    Forbidden,
};

[[nodiscard]] constexpr std::string_view to_string(PrivacyClass value)
{
    switch(value)
    {
        case PrivacyClass::Public: return "public";
        case PrivacyClass::PublicSummary: return "public-summary";
        case PrivacyClass::Sensitive: return "sensitive";
        case PrivacyClass::Private: return "private";
        case PrivacyClass::Forbidden: return "forbidden";
    }
    return "unknown";
}

enum class SupportLevel
{
    Candidate,
    Identified,
    ReadOnly,
    TelemetryQualified,
    LightingQualified,
    SettingsQualified,
    PairingQualified,
    ProductionQualified,
};

[[nodiscard]] constexpr std::string_view to_string(SupportLevel value)
{
    switch(value)
    {
        case SupportLevel::Candidate: return "candidate";
        case SupportLevel::Identified: return "identified";
        case SupportLevel::ReadOnly: return "read-only";
        case SupportLevel::TelemetryQualified: return "telemetry-qualified";
        case SupportLevel::LightingQualified: return "lighting-qualified";
        case SupportLevel::SettingsQualified: return "settings-qualified";
        case SupportLevel::PairingQualified: return "pairing-qualified";
        case SupportLevel::ProductionQualified: return "production-qualified";
    }
    return "unknown";
}

enum class ReceiverState
{
    Absent,
    DiscoveredReadOnly,
    Observed,
    ProfileQualified,
    SessionAuthorized,
    DegradedReadOnly,
};

[[nodiscard]] constexpr std::string_view to_string(ReceiverState value)
{
    switch(value)
    {
        case ReceiverState::Absent: return "absent";
        case ReceiverState::DiscoveredReadOnly: return "discovered-read-only";
        case ReceiverState::Observed: return "observed";
        case ReceiverState::ProfileQualified: return "profile-qualified";
        case ReceiverState::SessionAuthorized: return "session-authorized";
        case ReceiverState::DegradedReadOnly: return "degraded-read-only";
    }
    return "unknown";
}

enum class TransactionState
{
    Created,
    Validated,
    OwnershipBound,
    GenerationBound,
    Queued,
    Sent,
    HealthPending,
    Succeeded,
    Failed,
    Revoked,
};

[[nodiscard]] constexpr std::string_view to_string(TransactionState value)
{
    switch(value)
    {
        case TransactionState::Created: return "created";
        case TransactionState::Validated: return "validated";
        case TransactionState::OwnershipBound: return "ownership-bound";
        case TransactionState::GenerationBound: return "generation-bound";
        case TransactionState::Queued: return "queued";
        case TransactionState::Sent: return "sent";
        case TransactionState::HealthPending: return "health-pending";
        case TransactionState::Succeeded: return "succeeded";
        case TransactionState::Failed: return "failed";
        case TransactionState::Revoked: return "revoked";
    }
    return "unknown";
}

enum class LeaseState
{
    Requested,
    Granted,
    Renewed,
    Released,
    Expired,
    Revoked,
};

[[nodiscard]] constexpr std::string_view to_string(LeaseState value)
{
    switch(value)
    {
        case LeaseState::Requested: return "requested";
        case LeaseState::Granted: return "granted";
        case LeaseState::Renewed: return "renewed";
        case LeaseState::Released: return "released";
        case LeaseState::Expired: return "expired";
        case LeaseState::Revoked: return "revoked";
    }
    return "unknown";
}

enum class ResourceKind
{
    MouseLighting,
    KeyboardLighting,
    MouseSettings,
    KeyboardSettings,
    ReceiverPairing,
};

[[nodiscard]] constexpr std::string_view to_string(ResourceKind value)
{
    switch(value)
    {
        case ResourceKind::MouseLighting: return "mouse-lighting";
        case ResourceKind::KeyboardLighting: return "keyboard-lighting";
        case ResourceKind::MouseSettings: return "mouse-settings";
        case ResourceKind::KeyboardSettings: return "keyboard-settings";
        case ResourceKind::ReceiverPairing: return "receiver-pairing";
    }
    return "unknown";
}

enum class ErrorSeverity
{
    Info,
    Warning,
    Error,
    Critical,
};

[[nodiscard]] constexpr std::string_view to_string(ErrorSeverity value)
{
    switch(value)
    {
        case ErrorSeverity::Info: return "info";
        case ErrorSeverity::Warning: return "warning";
        case ErrorSeverity::Error: return "error";
        case ErrorSeverity::Critical: return "critical";
    }
    return "unknown";
}

} // namespace hyperflux
